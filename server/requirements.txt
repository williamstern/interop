enum34
Django>=1.8,<1.9
django-debug-toolbar>=1.3
django-sendfile
iso8601
matplotlib
python-memcached
networkx
numpy
pillow
psycopg2
pyproj
PyYAML # for loaddata
scipy
simplekml==1.2.7
ipaddress

# yapf is used for automated formatting of Python files.
# Since it can be finicky from version to version, we fix
# it at one version. Updating this is fine, but it may require
# reformatting some files.
yapf==0.6.2
